<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/feather-icons"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-[#1a0e2b]">

  <div class="flex flex-col md:flex-row rounded-3xl shadow-xl overflow-hidden max-w-5xl w-full h-[80vh]">

    <div class="md:w-1/2 p-12 flex flex-col justify-center bg-gradient-to-b from-[#2a1b4f] to-[#1a0e2b] text-white space-y-6">
      
      <h1 class="text-5xl font-extrabold bg-clip-text text-transparent bg-gradient-to-r from-orange-400 via-pink-500 to-purple-600 drop-shadow-xl">
        Marina
      </h1>

      <h2 class="text-xl text-gray-300 font-light">
        Gestão de Barcos e Tripulantes
      </h2>

      <p class="text-gray-300 mt-4">
        Gerencie seus barcos e tripulantes com facilidade e segurança. Acesse informações importantes de forma rápida e organizada.
      </p>

      <div class="flex mt-6 gap-4">
        <i data-feather="anchor" class="w-6 h-6 text-purple-300"></i>
        <i data-feather="users" class="w-6 h-6 text-purple-300"></i>
        <i data-feather="navigation" class="w-6 h-6 text-purple-300"></i>
      </div>

    </div>

    <div class="md:w-1/2 p-12 flex flex-col justify-center bg-[#1a0e2b]">
      <h2 class="text-2xl font-bold mb-6 text-center text-white">Login</h2>

      <% if(error){ %>
        <p class="text-red-500 text-center mb-4"><%= error %></p>
      <% } %>

      <form action="/login" method="POST" class="space-y-4">
        <input type="text" name="username" placeholder="Usuário" class="w-full p-4 rounded-xl border border-gray-700 bg-[#2a1b4f] text-white focus:outline-none focus:ring-2 focus:ring-purple-500">
        <input type="password" name="password" placeholder="Senha" class="w-full p-4 rounded-xl border border-gray-700 bg-[#2a1b4f] text-white focus:outline-none focus:ring-2 focus:ring-purple-500">
        <button type="submit" class="w-full bg-purple-600 hover:bg-purple-500 text-white p-4 rounded-xl font-bold transition-colors">Entrar</button>
      </form>

      <p class="text-center text-gray-400 mt-4">
        Não tem conta? <a href="./register" class="text-purple-500 hover:underline">Criar Conta</a>
      </p>
    </div>

  </div>

  <script>
    feather.replace();
  </script>
</body>
</html>
